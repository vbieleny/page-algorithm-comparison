/**
 * @file
 * @brief Functionality related to x86 memory map.
 */

#pragma once

#include <stdint.h>

/**
 * @brief Writes whole memory map to currently selected stream in user readable format.
 */
void memory_map_print();
